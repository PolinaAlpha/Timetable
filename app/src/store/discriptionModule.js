export const discriptionModule = {
    state: () => ({
        
      }),
      mutations: {
        tokenIn(state, token){
            state.token = token;
        }
      },
      namespaced: true
  }